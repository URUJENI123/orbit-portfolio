# TODO
- [ ] Reformat `src/components/About.tsx` to remove CRLF characters (`␍`) and apply Prettier wrapping so `prettier/prettier` ESLint errors are fixed.
- [ ] (Later) Optionally apply Tailwind canonical class suggestions in `src/components/Hero.tsx` if lint noise persists.

